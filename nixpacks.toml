[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["cd backend && pip install --upgrade pip setuptools wheel", "cd backend && pip install -r requirements.txt"]

[start]
cmd = "cd backend && uvicorn src.main:app --host 0.0.0.0 --port $PORT"
